#include "main.h"

/**
 * _isupper - checks for uppercase characters
 * Return: 1 is uppercase and 0 if not
 * @c: the character
 */

int _isupper(int c)
{

if (c >= 'A' && c < 'Z')

return (1);

else

return (0);
}
